@model IEnumerable<SegHig.Data.Entities.KPTrabajoTipo>

<table class="table table-striped table-responsive-md table-bordered smfont"  id="KPTrabajoTiposTable">
    <colgroup>
        <col span="1" />
        <col span="1" />
        <col span="1" />
        <col span="1" />
    </colgroup>
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Orden)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
             <th>
                Formularios
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Active)
            </th>
            <th>
                <a onclick="showInPopup('@Url.Action("AddTipoTrabajo", "KPTrabajoTipos", new { }, Context.Request.Scheme)' , 'Nuevo Tipo de Trabajo')" class="btn btn-sm btn-primary text-white"><i class="fa fa-plus-circle"></i> Tipo de Trabajo</a>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Orden)
            </td>
            <td>
                <a asp-action="DetailsTrabajoTipo" asp-controller="KPTrabajoTipos" asp-route-Id="@item.Id">@Html.DisplayFor(modelItem => item.Name)</a>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.KPFormularios.Count)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Active)
            </td>
            <td>
                <a onclick="showInPopup('@Url.Action("EditTrabajoTipo", "KPTrabajoTipos", new { id=item.Id }, Context.Request.Scheme)' , 'Actualizar Tipo de Trabajo')" class="btn btn-sm btn-warning"><i class="fa fa-pencil-alt"></i> </a>
                <button data-id="@item.Id" class="btn btn-sm btn-danger deleteItem" data-bs-toggle="modal" data-bs-target="#deleteDialog"><i class="fa fa-trash"></i></button>
                @if (!item.Active)
                     {
                       <a asp-action="OnOff" asp-route-id="@item.Id" class="btn btn-sm btn-outline-success"><i class="fa-solid fa-toggle-on"></i></a>
                     }

                @if (item.Active)
                     {
                       <a asp-action="OnOff" asp-route-id="@item.Id" class="btn btn-sm btn-outline-secondary"><i class="fa-solid fa-toggle-off"></i></a>
                     }
            </td>
        </tr>
        }
    </tbody>
</table>
