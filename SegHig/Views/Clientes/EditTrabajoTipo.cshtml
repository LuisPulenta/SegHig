@model SegHig.Models.TrabajoTipoViewModel

@{
    ViewData["Title"] = "Edit";
}

<h4>Editar Tipo de Trabajo</h4>
<hr />
<div class="row">
    <div class="col-md-12">
        <form asp-action="EditTrabajoTipo">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="ClienteId" />
            <partial name=_TrabajoTipo/>
            
            <div class="form-group mt-2">
                <input type="submit" value="Guardar" class="btn btn-outline-primary btn-sm" />
                <a asp-action="Details" asp-route-id=@Model.ClienteId class="btn btn-outline-success btn-sm">Regresar</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
